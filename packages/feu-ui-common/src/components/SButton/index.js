
import React from 'react';
import { Button } from 'antd-mobile';

export default class SButton extends React.Component() {
    render() {
        return (<Button type="primary" size="small" inline>small</Button>);
    }
}